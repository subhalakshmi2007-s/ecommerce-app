{
  "name": "ecommerce-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --no-audit --no-fund && CI=false node node_modules/react-scripts/scripts/build.js && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": "20.18.0"
  }
}